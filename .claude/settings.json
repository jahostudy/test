{
  "permissions": {
    "allow": [
      "Bash(git push origin main)",
      "Bash(git checkout main)",
      "Bash(git merge *)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Pushing or merging into main of jahostudy/test is allowed: the owner approves publishing test files to the live site."
    ]
  }
}
